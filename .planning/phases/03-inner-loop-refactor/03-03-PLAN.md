---
phase: 03-inner-loop-refactor
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "README.md contains a gzip FASTA limitation note near the database building section"
    - "The note explains the limitation and the workaround (decompress with gunzip)"
    - "No performance numbers are included in the note"
  artifacts:
    - path: "README.md"
      provides: "gzip FASTA limitation note for database builders"
      contains: "gzip"
  key_links:
    - from: "README.md gzip note"
      to: "NCBI or custom taxonomy based database section"
      via: "positioned near the Create a new database section"
      pattern: "gzip"
---

<objective>
Add a concise gzip FASTA limitation note to README.md near the database building section, explaining that gzip FASTA files fall back to slower sequential scanning and must be decompressed for large database builds.

Purpose: Satisfies DOCS-01. Users building large databases (core_nt scale) need to know compressed input files are not supported for random access, to avoid silent performance degradation or confusion.
Output: README.md updated with limitation note. No other files changed.
</objective>

<execution_context>
@/Users/carze/.claude/get-shit-done/workflows/execute-plan.md
@/Users/carze/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-inner-loop-refactor/03-CONTEXT.md

<interfaces>
<!-- README.md existing structure around the target insertion point -->
<!-- README.md lines ~414-418 (NCBI or custom taxonomy based database section) -->

The target insertion point is the "NCBI or custom taxonomy based database" section,
specifically the [!IMPORTANT] callout block listing three requirements for FASTA files.
The note should appear immediately after or as a fourth item in that block,
or as a separate [!NOTE] callout immediately after the [!IMPORTANT] block.

Existing [!IMPORTANT] block (lines ~414-418):
```markdown
>[!IMPORTANT]
Three requirements:
> 1. **FASTA files** : Each sequence must have a unique `>accession.version` or `>accesion` header
> 2. **NCBI-style accession2taxid** : ...
> 3. **NCBI-style taxonomy dump** : ...
```

User-specified tone (from CONTEXT.md decisions):
- Limitation + workaround (not a requirements statement)
- Example framing: "gzip FASTA files are not supported for random access. Decompress with gunzip before building large databases. Compressed files fall back to a slower sequential scan."
- No performance numbers
- Concise — one short paragraph or callout
- README only — no code comments
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gzip limitation note to README.md near the database building section</name>
  <files>README.md</files>
  <action>
Read README.md and locate the "NCBI or custom taxonomy based database" section, specifically around the [!IMPORTANT] block listing the three FASTA requirements (lines approximately 414-418).

Add a [!NOTE] callout block immediately after the closing line of the [!IMPORTANT] block. The note must:
1. State the gzip limitation (gzip FASTA not supported for random access)
2. Give the workaround (decompress with gunzip before building large databases)
3. Mention the fallback behavior (compressed files fall back to slower sequential scan)
4. Contain NO performance numbers or timing claims
5. Be concise (2-3 sentences maximum)

Insert the following callout immediately after the existing [!IMPORTANT] block:

```markdown
>[!NOTE]
> **gzip FASTA files**: gzip-compressed FASTA files (`.fa.gz`, `.fasta.gz`) are not supported for random access and fall back to a slower sequential scan. For large database builds, decompress your FASTA files with `gunzip` before running `metabuli build`.
```

This is a targeted insertion — do not modify any other content in README.md.
  </action>
  <verify>
    <automated>grep -n -i "gzip" /Users/carze/Documents/work/Broad_EMI/tools/Metabuli/README.md</automated>
  </verify>
  <done>README.md contains a gzip limitation note near the database building section. grep -i gzip README.md returns at least one match in the NCBI/custom taxonomy database building section. Note mentions decompress + fallback behavior. No performance numbers present.</done>
</task>

</tasks>

<verification>
1. `grep -n -i "gzip" README.md` — returns matches in the database building section
2. `grep -n "gunzip" README.md` — workaround command present
3. Manual review: note is positioned near the [!IMPORTANT] FASTA requirements block, is 2-3 sentences, contains no timing or performance numbers
</verification>

<success_criteria>
- README.md gzip note exists near the "NCBI or custom taxonomy based database" section
- Note states the limitation (no random access for gzip) and the workaround (gunzip before build)
- Note mentions fallback to sequential scan
- No performance numbers or timing claims in the note
- All other README.md content is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-inner-loop-refactor/03-03-SUMMARY.md`
</output>
